// Placeholder for useCart.js
// Full implementation will be added later.
import { useContext } from "react";
import { CartContext } from "../context/CartContext";

export default function useCart() {
  return useContext(CartContext);
}